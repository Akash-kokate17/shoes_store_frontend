import { FilterMinValArrPipe } from './filter-min-val-arr.pipe';

describe('FilterMinValArrPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMinValArrPipe();
    expect(pipe).toBeTruthy();
  });
});
