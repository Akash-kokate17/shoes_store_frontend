import { FilterMaxvalarrPipe } from './filter-maxvalarr.pipe';

describe('FilterMaxvalarrPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMaxvalarrPipe();
    expect(pipe).toBeTruthy();
  });
});
